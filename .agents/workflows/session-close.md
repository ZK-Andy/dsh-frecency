# 会话收尾检查单（session-close）

> 每个开发批次完成或会话结束时执行（可多轮触发，由 feature-flow 步骤 7 调用）；全部完成才算收尾。

1. **提交对账**：`git log` 本会话全部提交 vs HANDOFF 最新记录——每条提交必须能对应到交接条目。
2. **决策落档（结论只落 durable 家）**：本轮拍板结论是否已进 ADR / `docs/cookbook.md` / README / AGENTS？没有则补。**结论只落 durable 四家，不落 HANDOFF 滚动窗**——滚动窗不复述结论；凡待跨会话用的复现细节/取证点/判据，正文进 durable 家，滚动窗与待办区只留指针。
3. **待办对账**：HANDOFF 待办区（`HANDOFF-todos.md`）增删——本轮产生的跨会话遗留/观察/待复现项必须写进待办区（`[ ]` 条 ≤340 字、`[x]` 条压缩为一行指针），不留滚动窗副本。
4. **README 同步（强制）**：核对 README 与 shipped 现实；有漂移即同步修正并随本批次提交，无漂移也在收尾汇报中显式说明「README 核对无变更」。
5. **未推送提醒**：本地领先 origin 的提交数如实告知用户；是否推送由用户定。
6. **工作树确认**：`git status` 干净；untracked 仅限预期清单（HANDOFF.md、`.plan/` 等）。
7. **交接条目**：HANDOFF「交接更新记录」滚动窗追加本会话**精条目**——日期｜类型｜指向 ADR（一行主题，不复述结论）｜关键提交哈希｜README 同步结论一行。**前置判据（fail-loud）**：凡本轮拍板的结论必须已在步骤 2 落 durable 家；未落则先补 ADR，不得以收紧条目代替。
8. **收尾汇报**：向用户汇报，附待办全景速览；README 核对结论（变更或「无变更」）一并说明。
